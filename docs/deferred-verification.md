# Deferred verification ledger

**This file is appended by every phase and worked through at the end. It is
the only place a deferred check is recorded.**

A phase is done when the thing it built works. Some acceptance lines cannot be
proved when the phase that owns them is built — they need a live vendor
account, or a domain a later phase creates. That is normal and it is not a
defect. What is dangerous is recording such a line as *met* and moving on,
because nothing then remembers to go back.

So each one becomes a row here, with the specific thing that closes it.

## The rule

- **A phase never records one of these as met outright, and never as "checked
  later".** It records it as met **for the mechanism**, or **against the file
  transport** — in those words — and adds a row here.
- **Phases add rows. A phase does not close its own row** — the row is closed
  by whoever runs the real check, at the event or phase named in *Closed by*.
- **Closing means someone ran it**, and the row says what they saw. A row
  closed by reading the code was never open.
- **If a line is testable now, test it now.** Deferring something that could
  have been checked is the failure this file exists to prevent. A row is for
  what is impossible today, not what is inconvenient today.

## Open — closed at deployment

These need a live AWS account. Deferred by the owner on 2026-09-06; the plan
records the decision at §0.0 item 17 and
`docs/decisions/11-email-deferred-to-deployment.md` explains it. The runbook
to work through is `docs/ops/ses-production-access.md`.

| # | What is unverified | Closed by |
|---|---|---|
| L1 | A real send through the SESv2 API. `SesEmailTransport` is unit tested against a fake. | One OTP to a mailbox you control, then `email_message.status = delivered`. |
| L2 | A live SNS notification reaching `/v1/webhooks/ses-events`. Signature checking is tested against a test-generated RSA keypair. | A growing `email_event` count after a real send. |
| L3 | The `SubscriptionConfirmation` handshake. No real `SubscribeURL` has been fetched. | The log line `sns subscription confirmed`, once. |
| L4 | Suppression on a real hard bounce. | Send to `bounce@simulator.amazonses.com`; a row appears in `email_suppression`. |
| L5 | Suppression on a real complaint. | Send to `complaint@simulator.amazonses.com`; same check. |
| L6 | Domain deliverability — SPF, DKIM, DMARC alignment and the custom MAIL FROM. | The receiving mailbox's headers show all three passing. |
| L7 | That the three configuration sets keep their reputation metrics apart. | Three sends, one per channel, each landing under its own set in the SES console. |
| L8 | The OTP mail reaching a real inbox with the six-digit code readable, and the sender/subject rendering as intended. Phase 3 verified the whole register → verify → login cycle against `EMAIL_TRANSPORT=file`. | Register with an address you control after L1; the code from that inbox verifies the account. |
| L9 | A Flutter exception reaching Sentry. The Phase 3 `CrashReporter` interface and its no-op-without-`SENTRY_DSN` implementation are built and tested (`frontend/lib/core/crash/`), matching Phase 2's backend posture, but nothing has run against a real Sentry project — this build has no real DSN. | A forced test exception in a build with a real DSN appears in the Sentry project within minutes. |
| L10 | The password-reset mail reaching a real inbox with its six-digit code readable, and its subject rendering as intended. Phase 3b verified the whole request → verify → confirm → sign-in cycle against `EMAIL_TRANSPORT=file`, reading the code out of the written message. | After L1, request a reset for an address you control; the code from that inbox sets a new password. |
| L13 | **A listing image living in a real object store, and a real presigned PUT.** Phase 8 built `MediaStorage`, the three-step upload, server-side content-type sniffing, the size ceiling and EXIF stripping, all verified **against the file transport** (`MEDIA_STORAGE=file`, with an in-memory variant in the suite) — the stripped bytes are read back out of the store and asserted to no longer carry the location. What a local directory cannot prove is that a client PUTting **straight to the vendor** works, that the vendor honours the signed terms, and that `finalise` can read the object back and rewrite it over the network. `MEDIA_STORAGE=s3` is refused at config load until this closes, and `file` is refused in production, so **production cannot boot until the bucket lands** — deliberate, endorsed 2026-09-10, and no new blockage: production is already unbootable without procurement, since `EMAIL_TRANSPORT` must be `ses` there and SES production access is itself still pending. §Phase 24 is the phase that meets both. | Create the bucket, build the S3 transport behind the existing `MediaStorage` interface, and upload a real phone photo end to end: the PUT reaches the bucket without touching the API, `complete` returns `stored`, and the object downloaded from the bucket carries no EXIF. Then check the same for a file whose declared type is a lie, and that the read URL stops working when it expires. |

## Open — closed by a push vendor

Separate from the AWS rows above because they close on different accounts, at
different moments, and neither is AWS. §Phase 3c's first Done-when clause — "a
test push arrives on a real device within seconds" — is recorded as met
**against the fake**: the interface, registration, refresh, multi-device
fan-out, token cleanup, the OS-denied state and every rung of the fallback
chain are all built and tested, asserting what the sender was asked to do. A
real device is the one thing a fake cannot prove.

Deferred by the owner on 2026-09-08, before the phase started;
`docs/decisions/15-phase-3c-push.md` explains it. `PUSH_TRANSPORT=fcm_apns` is
refused at config load until these close.

| # | What is unverified | Closed by |
|---|---|---|
| L11 | **A real push arriving on a real Android device via FCM.** Phase 3c built `PushSender`, the transport boundary, device registration/refresh/multi-device, the OS-permission-denied state and every rung of the fallback chain against a fake (`RecordingPushTransport`), asserting what the sender was *asked* to do. No Firebase project exists and Phase 3c deliberately procured none (`docs/decisions/15-phase-3c-push.md`). | Create a Firebase project, build the FCM transport behind the existing `PushTransport` interface, register a real device, and see a test push arrive on it within seconds — then its ack land at `POST /v1/push/dispatches/:id/ack` and the 30-minute fallback email NOT go out. |
| L12 | **A real push arriving on a real iOS device via APNs.** Same build, same fake; a separate row because an Apple developer account carries a fee and a lead time a Firebase project does not, so this will close later than L11. | An APNs key on a paid Apple developer account, the APNs transport behind the same interface, and a test push arriving on a real iPhone within seconds, with the same ack assertion as L11. |

## Open — closed by a later phase

| # | What is unverified | Closed by |
|---|---|---|
| P1 | A deleted account's reviews remain with anonymised attribution. Phase 3 built `AnonymisationHooks` and tested that a registered hook runs in the anonymisation transaction. | Phase 11 registers the review hook and its test asserts a review survives with the author anonymised. |
| P4 | **A category's `bookingMode` cannot change once it has published slots or live bookings** (plan §Sequencing, Round 15 follow-ups). Phase 4 seeds the mode and `PATCH /v1/admin/categories/:id` accepts a change to it unconditionally, because there is no `Listing` and no `Booking` to check against — the refusal has nowhere to read from. Invariant 4 means it has to be a server-side refusal when it lands, not an admin-UI control. | Phase 9a (published slots) and Phase 17 (live bookings) give the check something to read. Whichever lands second adds the refusal to `CategoryService.update` and a test that seeds a category, gives it a published slot or a non-terminal booking, and sees the mode change rejected — plus the same question for deactivating a category that still carries live listings. |
| P5-2 | **§1f's conduct numbers are the ones the booking log actually produces.** Phase 5 built the read surface — the DTO shape, the ten-completed-booking floor measured against the 90-day window, the provider-sees-their-own-first rule, and the structural absence of any field an editorial label could occupy — all **against the `ProviderConductSource` seam** (`FakeConduct`). No metric has been computed from a booking, because there are no bookings. | Phase 11 implements `ProviderConductSource` over the booking event log and its test drives real bookings to terminal states, then asserts each of the seven §1f definitions against the resulting numbers — in particular that a customer cancellation does not move `cancellationRate`, that a timeout feeds response rate and not acceptance, and that on-time covers emergency against the accepted offer's `etaMinutes` (Round 22). |
| P5-3 | **The three consumers named in §Phase 5's suspension Done-when reach the rule through the one helper.** "Verified from search, Home, and the public profile in one test" names Phases 15, 16 and 13, none of which exists. Phase 5's test exercises the three *shapes* — a filtered page, an unfiltered page and a by-id check — and asserts all three exclude a suspended provider. What it cannot assert is that the eventual endpoints call this helper rather than rebuilding the rule. | Whichever of Phases 13, 15 and 16 lands last adds a test that suspends a visible provider once and asserts they disappear from all three real endpoints — and a check that no consumer reimplements the published-listing count or adds its own suspension filter (§1a: suspension is an input to the helper). |
| P5-4 | **That the payment-step exception is actually scoped to a booking.** §Phase 5's last Done-when allows payment details in exactly one place, and `paymentDetailsForBooking` is that place — but it takes no viewer on purpose, because the booking-scoped authorization belongs to the phase that can see a booking. Phase 5 asserted the line by calling the accessor directly, which proves the *mechanism* (nothing else in the module can reach the fields) and proves nothing about the scope. | Phase 17.1 calls it from the payment step only, after authorizing the caller against the booking, and its test asserts: the customer on the booking gets the details at `awaiting_payment`; a signed-in stranger gets 403/404 on the same booking; no other booking endpoint's response carries them at any status; and no route exposes the accessor. |
| P6-2 | **That Profile's three unbuilt rows reach the screens they name.** Saved, Saved preferences and Help & support navigate — each to an `UnbuiltScreen` carrying its own title and owing phase, asserted per row — but a placeholder is not the screen. | Phase 14 (Saved), Phase 19b (Help & support) and — 🔧 **reattributed 2026-09-10, settled by the owner** — **Phase 17.4** for Saved preferences. This row said Phase 7, and §Phase 7's own bullets and Done-when name it nowhere: it is a §Phase 3 account-settings bullet Phase 3 deferred because it needs `Island`. Phase 7 seeded `Island` and built the picker, so the screen is now buildable, but its entity shape and endpoints are specified in no section of the plan and Phase 7 declined to invent them (`docs/decisions/19-phase-7-service-areas.md` §1). The route now reads `owedBy: 'Phase 17.4'` — §1h's own framing is that saved addresses, preferred windows and standing instructions are "reused across bookings" and "carried forward by Book Again", which is 17.4's slice, and before bookings exist a saved preference has nothing to be used by. Phase 14's own Done-when adds "Profile's count updates", which is also when `profile-summary` gains its first count. |
| P6-3 | **That the four booking tiles land on the tab each one names.** Round 48 §2's defect was four labels reaching one screen, so each tile has its own destination and the test asserts all four are distinct — but all four are placeholders today, and `My Bookings` has no tabs to deep-link into. | Phase 17 builds My Bookings with its four tabs and wires each tile to its own, then asserts the four land on four different tabs rather than four different placeholders. |
| P6-4 | **That a customer can change their own name from the app.** `PATCH /v1/users/me` is built and tested end to end (`backend/test/customer-profile.test.ts`) — it is met **for the mechanism**. It has no caller: neither `Profile_customer.jpg` nor `Profile.dc.html` carries a name-edit control, so Phase 6 built none rather than inventing one (`docs/decisions/18-phase-6-customer-profile.md`, decision 3). | A design round adding the affordance to `Profile.dc.html`, then the phase that implements it. Until then the endpoint is reachable only by an API client, which is a real gap in the product and not a defect in this phase. |
| P6A-1 | **That Home's "Become a Provider" CTA reaches the intro screen.** §Phase 6a's first Done-when names two entries — "Home's 'Become a Provider' CTA **or** Phase 6's role switcher". The switcher half is built and asserted through the real route table; the Home feed does not exist, so the CTA has nowhere to live yet and Phase 6a built none rather than inventing a Home surface. Met **for the mechanism**: the route, the resume calculation and everything behind them are the same for either entry. | Phase 16 builds the Home feed. Its test should push the CTA and assert it lands on `BecomeProviderScreen` at the step the account's state implies — step 1 for a customer, step 3 for a provider who stopped there — reaching the same `RoleSwitch` decision rather than a second copy of it. |
| P7-2 | **That the register survives an update.** The seed is create-if-absent for the row and recompute-always for the derived columns, and `islands-seed.test.ts` asserts every part of that against the register as it stands today — including that a stale `nameAmbiguous` is corrected in both directions. What no test covers is a **real** register revision: an island added, lost, or respelled. A respelling in particular creates a second row by design (`seedKey` is `"<atollAbbr>:<name>"`), which is a deliberate refusal to guess at an identity decision, not a defect. | The next time `docs/data/inhabited-islands.json` is re-extracted. Diff it against the current file first, run `npm run db:seed`, and check its reported counts against that diff — every creation should correspond to a real addition or a known respelling, and any `Ambiguity changed on:` line should name a pair the diff explains. Deactivate a superseded row through Phase 10b rather than editing the seed. |
| P7-3 | **That a service area is what discovery actually matches on.** 🔧 **Half advanced by Phase 8, 2026-09-10.** The listing half is now real: `ListingServiceArea` exists, is keyed on `islandId`, soft-deletes and revives on the unique `(listingId, islandId)` pair, and `test/listings-service-areas.test.ts` shows a provider whose **account default covers Malé while their listing does not** — the two sets live in different tables and neither write touches the other. What is still unverified is the clause the row was written for: that the island **filter** reads the listing's set. There is no search to read either set. | Phase 15's island filter. Its test should take the fixture this phase already builds — account default includes an island, listing does not — and confirm the listing is what decides whether the customer sees it, matching on `islandId` and never on a name (§0.0 item 12). |
| P8-1 | **That `priceUnit` is narrowed per category, as §Phase 8's bullet says to seed it.** The closed five-value enum is built and enforced, so the failure the bullet exists to prevent — `/session`, `/sesion` and `/per session` on three listings — cannot occur. Met **for the guard**. What is unbuilt is the per-category *subset*, and it is unbuilt because no subset exists to build: this document names none, the wizard prototype's `UNITS` is a flat five rendered whatever the category (in a file that keys its tag suggestions by category, so the omission reads as deliberate), and §Phase 10b's editable Category fields are enumerated without it. Inventing twelve lists would silently block a legitimate provider at publish. | A config round that decides the subsets and gives §Phase 10b the surface to hold them. It should add `Category.priceUnits` then — one additive column with a constant default, so nothing is owed to it now — and its test should show a unit valid on one category refused on another, which is the only assertion that distinguishes a real narrowing from a column nobody reads. |
| P8-2 | **§Phase 8's soft-delete cascade rules, against real bookings, reviews and reserved slots.** 🔧 **The slot third is now real — Phase 9a, 2026-09-14.** `test/availability-rules.test.ts` deletes a listing that carries a reserved future slot and asserts all three clauses of `docs/decisions/21-phase-8-service-listings.md` §7: the deletion is **not refused**, the picker answers not-found for the listing outright, the next generation withdraws every future *unreserved* slot, and the **reserved** slot survives untouched with its reservation still held. What remains unasserted is the booking and review two thirds — there is no `Booking` until §Phase 17 and no `Review` until §Phase 11. | Whichever of Phases 11 and 17 lands last: delete a listing that carries a confirmed future booking and a completed booking with a review, then assert the booking still completes with its chat and attestation and the review still counts toward the provider's conduct — and that **no phase added a refusal** blocking deletion while bookings are open. |
| P8-3 | **That a tier drop actually clears an emergency listing's flag in production, and that the provider is told.** §1c (Round 17) requires a downward tier change to re-check every published `isEmergency` listing, clear the flag where the category's bar is no longer met, and **notify the provider naming the listings and the reason**. `ListingService.reevaluateEmergencyEligibility` is built and tested — gold→silver clears an Electrical listing and leaves an AC Repair one alone, which is the per-category half — but it is **called from no route**: tier changes are §Phase 10a's verification queue, which does not exist. The notification is not built at all; §Phase 19 owns notifications and the copy is unspecified. | Phase 10a's verification queue calls it inside the same transaction as the tier change, and Phase 19 supplies the notification. The test should demote a gold provider holding one live emergency Electrical listing and one live emergency AC Repair listing, and assert exactly one flag cleared, the listings still published, an audit entry, and one notification naming the affected listing. |

| P8A-2 | **The confirmed-booking trial trigger and the downgrade listing-protection, against real bookings.** §Phase 8a's Done-when opens with "a trial starts on first confirmed booking" and adds "an admin resolving `payment_unresolved` to `confirmed` fires the trial hook"; §1b protects a listing with a non-terminal booking and a future `scheduledFor`. There is no `Booking` until §Phase 17.1, so both run against one injected source (`SubscriptionBookingSource`, defaulting to `NO_BOOKINGS`, driven in the suite by `FakeBookings`). Met **for the mechanism**: `onBookingConfirmed` starts a trial, is the same `startTrial` the explicit endpoint reaches, takes one argument so no caller can wire a path-specific variant, and never produces a second trial; the protection keeps a committed listing visible over the cap and lets it go the moment the fake reports the booking terminal. The ranking beside it *is* proven against real rows — `ListingEvent` exists, so booking and view events are seeded and counted over §1b's 90-day window. | §Phase 17.1 implements both methods over the real table, calls `subscriptions.onBookingConfirmed` inside the state transition (not from an endpoint) and `subscriptions.reconcileVisibility` on a terminal one. Its test should: drive one booking to `confirmed` through the customer path and assert a trial started; drive a second provider's booking to `payment_unresolved` and have an **admin** resolve it to `confirmed`, asserting the same hook fired; and take a provider over their cap with a confirmed future booking, complete that booking, and see the protection lapse on the next sweep with the higher-performing listing restored in its place. |
| P8A-3 | **That a provider is actually told about any of §1b's billing events.** The five §1b requires — the 7-day trial and subscription warnings, `downgraded_to_free`, and win-back at 7 and 30 days — plus the confirm/reject outcomes, the "Try Premium" prompt and the introductory-price notice all fire at the moments §1b specifies, and the suite asserts each fires **once** however often the hourly job runs. What they reach is `BillingNotifier`, whose default logs `delivered: false` and drops: §Phase 19 owns notification content and five of these are already in its type list by name, so writing the copy here would put it in two phases. Met **for the mechanism**, not for delivery. 🔧 Two events have **no §Phase 19 type at all** — `trial_prompt` and `introductory_price_converting` — and that gap is part of this row. | §Phase 19 registers a real notifier and adds the two missing types (or names them differently and says so). Its test should assert each event goes out through §Phase 3c's sender exactly once, that a downgraded provider gets both win-backs and not a third, and that the price-conversion notice is not delivered on the trial-ending template — a provider whose bill is about to double should not be told through the wrong one. |
| P9-1 | **That the wizard's offering-name guidance stays attached to the two categories it belongs to.** §Phase 9's Round 25/26 line gives Photography and Boat Charter a different step-1 name helper — *"Name the specific service… Customers book the offering, not the category"*. The plan names those two by **name** and seeds no flag, so Phase 9 enumerated them in Flutter, keyed on `iconIdentifier` rather than the display name on the reasoning that the token survives a rename (Computer → Appliance Repair). **The key is not stable either**: §Phase 10b makes the icon admin-editable and `iconIdentifier` is in the admin patch schema, so changing a category's icon would silently change its naming advice — worse than a rename, because nothing about editing an icon suggests you are editing copy. Latent today: no admin panel exists and the seed is the only writer. | **§Phase 10b, before it ships the category editor.** The fix is a `Category.listingGuidance` column seeded for the two — the same shape as `suggestedTags`, which §Phase 8 added for exactly this reason — after which the enumeration leaves Flutter entirely. `Category.seedKey` is the only stable identity on the row today and is not on the DTO, so there is no smaller fix that is also a real one. Whichever phase adds the column should assert that renaming a category and changing its icon both leave the guidance where it was. |
| P9-2 | **The media step against a real photo library and a real presigned PUT.** §Phase 9 drives `MediaPicker`/`MediaUploader` through fakes, so the three-step upload, the content-type sniff, the size ceiling and EXIF stripping are all verified — but the `image_picker` platform channel and the actual PUT to `/v1/media/uploads?token=…` are not, and a cover image is one of the six fields publish requires. The 2026-09-14 device pass reached the wizard and verified the handoff, the header count and the offline queue, but **not this**: once onboarding is complete there is no route to the wizard at all — §Phase 10's My Services is an `UnbuiltScreen` and `/services/new` is reachable only from onboarding's confirmation sheet — so a second run needs a second account. 🔧 **That obstacle is gone — §Phase 10, 2026-09-15.** My Services is a real screen and reaches the wizard twice over: "New Service" opens a fresh draft and the card menu's Edit/Continue editing resumes an existing one, both through the real route table. The device pass this row asks for no longer needs a second account, and the re-entry half can be seen on the same run. | A device pass that registers a fresh provider, reaches step 4 and picks a real photo: the PUT lands, `complete` returns `stored`, the cover renders, publish stops refusing the cover field, and the stored bytes carry no EXIF. Cheapest once §Phase 10 gives the wizard a second entry point, which also closes the re-entry half. Fold in a re-check of the cold-start queue drain fixed the same day, which is covered by a test but was not re-seen on a device after the emulator went away. |
| P1-3 | **A spacing system in the artboards themselves.** The 61 prototypes were drawn on a 1 dp grid and never had one: 13 px appears **172** times across them, 11 px 159, 9 px 112, 15 px 83. Flutter reproduced them faithfully, so the app's nine traced values (`AppSpacing.n5` … `n34`) are correct rather than drift — naming them on 2026-09-14 removed the last arithmetic and changed no pixel, but it did not give the design a scale. | A design round that rationalises spacing across the artboards — roughly 750 values — after which the traced group is deleted and the scale is the whole vocabulary. Worth doing when a round is touching those screens anyway rather than as a sweep: the app has to follow the artboards here, not lead them, so every value moved is a screen that must be re-imported and re-verified. Until then `n13` reads as *measured*, which is the honest claim. |
| P9A-3 | **A §Phase 21 alert on slot-generation overrun.** §Phase 9a asks for "a stated wall-clock budget and a §Phase 21 alert on overrun". The budget is stated and enforced — 5 seconds and 50 listings a run, in `modules/availability/generation.ts`, with the reasoning beside it — and on overrun the job emits a structured `slot_generation_overrun` warning carrying the budget, the elapsed time, how many listings were processed and the slowest one. Nothing is lost on an overrun: `ListingSlotState` is a durable work list and the next tick resumes from it. Met **for the signal**; there is no alerting to route it to, because §Phase 21 is where observability is built. | §Phase 21 wires `slot_generation_overrun` to a real alert, alongside the `payment_unresolved` ageing alert §Phase 10a leaves in the same state. Its test should trip the budget with a low limit and assert the alert fires once, not per listing. |
| P10-DEFER | **The whole admin panel, deferred by decision rather than by dependency (2026-09-16, §0.0 item 20).** §Phase 10a Part 2 and §Phase 10b are paused — not started, no stack proposed, no `admin/` directory — and the build moved to §Phase 11. This is not a deferred *verification* in the usual sense: nothing here is impossible to check, it simply has not been built. It is recorded here because the consequence is invisible in the code and easy to lose. **With no panel, no `PaymentSubmission` is ever confirmed and no provider ever reaches Bronze**, so nobody becomes paid and nobody becomes verified. Every test and fixture sets both directly, so development sees nothing wrong; a launch would see nothing work. §Phase 10c is **not** part of this — it was already sequenced behind §Phase 21's `ProductEvent` log and is where it should be. | Whoever restarts the admin panel. The order to return to is §Phase 10a Part 2, then §Phase 10b, which is what §4's sequencing note says and what §0.0 item 20 amends only for the present. Close this when both are built and §Phase 10a's remaining Done-when clauses (ledger P10A-1) are asserted — the two rows close together or not at all. |
| P10A-1 | **The half of §Phase 10a's Done-when that needs the admin panel.** Part 1 is built and two of the six clauses are met end to end — "a provider submits with proof and sees pending" and "a rejection surfaces its reason with working resubmit and appeal", both through the real route table. Four are Part 2's and nothing here asserts them: **an admin confirms and the entitlement activates** (the endpoint is §Phase 8a's and is tested there, but no panel calls it), **CSV import proposes correct matches**, **the three XSS payloads render inert in every admin view** — there is no admin view — and **an aged `payment_unresolved` item triggers its alert**, whose queue does not exist either. The owner paused Part 2 on 2026-09-15; this is not a defect in Part 1, it is the phase being half-built on purpose. | §Phase 10a part 2, whenever the owner calls it. Its test should confirm a real submission through the panel and see the provider's entitlement change; import a statement CSV and see the matcher's proposals; plant `<script>`, `<img onerror>` and a `javascript:` URL in a listing description, a review body and an enquiry message and see all three inert in every view that renders them; and age a `payment_unresolved` item past five business days and see the alert fire once. 🔧 Note that plan revision 5.32 (§0.0 item 19) added a **full-match auto-confirmation** since this row was written, so "an admin confirms" now has a second path that confirms with the **system** as actor — both need asserting. |
| P10A-2 | **The invoice PDF downloading from a real object store.** §Phase 8a writes the bytes and stores them through `MediaStorage`; §Phase 10a part 1 built the list and the per-invoice download, and the fetch, the temp-file write and the hand-off to the OS share sheet are all asserted through a real `ProviderContainer` against a mock HTTP client (`frontend/test/features/billing/invoices_screen_test.dart`). Met **against the file transport**: what a local directory cannot prove is that a **short-lived signed URL from the real store** is reachable from a phone, that its expiry actually bites, and that the PDF arrives intact over a network rather than out of `backend/.media/`. Shares ledger row **L13**'s blocker — `MEDIA_STORAGE=s3` is refused at config load. | The same event that closes **L13**: the bucket lands and the S3 transport goes in behind `MediaStorage`. Then download an invoice on a real device: the PDF opens in the OS viewer with the right number and amount, and the same URL stops working once it expires. |
| P10A-3 | **The receipt picker and the presigned PUT, on a real device.** §Phase 10a part 1 drives `MediaPicker` and `MediaUploader` through fakes, so the three-step upload, the type check, the size ceiling and the server-side EXIF strip are all verified — but the `image_picker` platform channel and the actual PUT are not, and a receipt is a photo of a bank-app screen taken on a phone. This is §Phase 9's **P9-2** in a second place: the same two seams, a different screen, and the same thing unproven. | A device pass that reaches Billing → Pay by bank transfer on a provider account, photographs or screenshots a receipt, and submits: the PUT lands, the submission flips to pending, and the stored bytes carry no EXIF. Cheapest folded into P9-2's pass, which needs the same emulator and the same provider account. |

| P17-1 | **That anybody is actually told a booking changed.** §Phase 17.1 fires every event §1c names — accepted, payment claimed, claim withdrawn, receipt confirmed, seven-day escalation, dispute, completion prompt, auto-completion, cancellation, amendment proposed and answered — through `BookingNotifier`, and the default **logs and drops**. §Phase 19 owns notification content and writing copy here would put it in two phases, which is the mistake §Phase 8a's `BillingNotifier` note already names. The **one** exception goes out for real: the provider's accept prompt dispatches through §Phase 3c's `NotificationDispatcher`, because that phase built `booking_accept_prompt` and a booking-shaped context for exactly this. Met **for the mechanism** — the events fire at the right moments and nothing pretends a person was told. | §Phase 19, which registers a real notifier and gives each event a type. Its test should drive a booking from `requested` to `completed` and assert each event reached §Phase 3c's sender once — and that the seven-day escalation reaches **both** parties, which is the only one that is not addressed to a single person. |
| P17-2 | **The accept prompt landing on a real device from a real push.** §Phase 3c's fallback chain is exercised against `RecordingPushTransport` and this phase dispatches through it, so what is asserted is that the prompt is *addressed* — the right provider, the right context, standard urgency, and no amount or phone number in the copy. What no test here can prove is that a provider whose phone is asleep on a weak atoll connection actually sees it in time to answer inside the 24-hour window, because no FCM or APNs account exists (`docs/decisions/15-phase-3c-push.md`). Met **against the recording transport**. | The same event that closes §Phase 3c's own device rows: a real vendor project, a real device, and a booking created from another account. Watch for the notification, tap it, and land on the accept prompt with the countdown running. |
| P17-3 | **That the offline accept survives the app being killed, not just the connection.** `OfflineQueue` writes a queued accept to disk as data rather than a closure precisely so a cold start can replay it, and `replay_on_start_test` already proves that mechanism for the wizard. This phase asserts the booking half against a fake store: queued on refusal, merged on a double tap, sent exactly once on reconnect, never reported as success. Met **for the mechanism**; what is unproven is the real case — Android reclaiming the app while it is backgrounded on a provider's phone, mid-atoll, and the accept going out on the next launch. | §Phase 9's **P9-2** device pass, which needs the same emulator: accept a booking in airplane mode, force-stop the app, restore the connection, reopen it, and see the booking accepted on the server with exactly one accept in the log. |
| P17-4 | **That the `booking` thread actually opens when a quote is offered.** §Phase 17.2's Done-when puts the chat at `quote_offered` rather than `accepted`, and splits the work: "17.2 owns the state, §Phase 18 owns the thread". There is no `Conversation` model yet, so what is asserted is the **state** — `bookingChatState` derives `not_open` / `open` / `locked` from `quoteOfferedAt`, `amountSetAt`, the status and `completedAt`, and `chatState` carries it on every booking DTO. `test/phase17-2-done-when.test.ts` sees it flip to `open` on the quote and not before, for both parties, and stay open through approval; the Flutter screens offer the way in and say plainly that messaging lands with the messaging module rather than pretending to open one. Met **for the mechanism**: no message can be sent, and nothing here claims one can. | §Phase 18, which builds `Conversation` and its two types. Its test should offer a quote on a `request` booking and assert a `booking`-type thread exists and accepts a send at `quote_offered` — one whole state before `accepted`, which is the point of the clause — and that a slot booking's thread does **not** exist until `accepted`. Round 27's tail is the same row's: a thread still sending six days after completion and read-only on the eighth, with a dispute reopening it. |


## Closed

Rows move here with what was actually seen, so the ledger reads as a record
afterwards rather than an empty promise.

| # | What was unverified | Closed by | Closed on | What was seen |
|---|---|---|---|---|
| P6-1 | **That the role switcher's later switch actually reaches the dashboard.** Half of it closed with §Phase 6a, when `/become-a-provider` became a real screen; what remained was §Phase 10's, because `/provider/services` still resolved to an `UnbuiltScreen` naming the phase that owed it. | Phase 10 | 2026-09-15 | **Both branches asserted in one place, as the row asked.** `frontend/test/features/my_services/phase10_done_when_test.dart` boots the **real app and its real route table**, pushes Profile, opens the role switcher and taps Provider: a provider whose `providerOnboardingComplete` is true lands on `MyServicesScreen` with their listings rendered and **no `UnbuiltScreen` anywhere in the tree**; one whose is false still lands on `BecomeProviderScreen`. Asserting the pair in one test is what the row asked for and is what stops either branch being verified in isolation while the other rots. The screen behind it is not a shell: the same file asserts §Phase 10's three Done-when clauses through the same route table — every context-menu action performing a real mutation with no re-listing, a drafts-only provider's zero state, and a `silver` badge surviving a degraded entitlement. |
| P9A-4 | **That `npm run lint` passes on this machine.** §Phase 9a's six new models took type-aware ESLint past 6.3 GB peak RSS, where the same lint on the pre-9a tree passed under the default 4 GB. The phase measured it to the schema — a worktree at the previous commit lints clean, and the regenerated `schema.prisma` with none of the new TypeScript reproduces it — and left the lever to the owner, because each candidate changed shared lint configuration or the schema's integrity guarantees. | Verification, 2026-09-15 | 2026-09-15 | **No lever was needed. It was one rule meeting one line.** Bisecting the 88 type-aware rules with `eslint --rule '{"<rule>":"off"}'` found `@typescript-eslint/no-unnecessary-type-assertion` alone responsible: with it off and every other rule on, `eslint src` passed in 19 s at 2.1 GB. Bisecting files found one — `src/modules/listings/service.ts`, §Phase 8's, 675 lines, **4.35 GB and a heap abort by itself**. Bisecting its three assertion sites found the pair on line 180: `data[key as keyof Prisma.ListingUncheckedUpdateInput] = value as never`. **Either assertion alone is fine** — `keyof` without `as never` is 1.7 GB, `as never` without `keyof` is 0.5 GB — and together they are 4.35 GB and an abort, because the rule must weigh `never` against the union of every field's update-operation type. Writing the loop through a plain `Record<string, unknown>`, with `data` keeping its Prisma type for the call site, put **`npm run lint` at 13.4 s and 1.06 GB with all 88 rules on**, `tsc --noEmit` clean, and all 633 backend tests passing. **The phase's own reasoning was sound and its conclusion was not:** loading the enlarged type surface is nearly free — type-aware parsing with no type-aware *rules* running is 3.4 s and 0.68 GB — so the schema was the trigger, never the cost. Each proposed lever (a bigger CI heap, dropped `strictTypeChecked` rules, a thinner Prisma relation surface) would have paid permanently for a local cause. Note that an `eslint-disable` comment could not have helped either: it filters what a rule reports, after the rule has already run. `scripts/verify.sh` is green end to end. |
| P1-2 | **`fadeUp` adopted everywhere the artboards specify it.** `motion.css` defines the entrance and 52 of the 61 prototypes use it, 194 times; the app implemented none of it until 2026-09-14. The primitive and the screens built after it were done; the screens built before it were not. | Verification, 2026-09-15 | 2026-09-15 | **Closed by the owner noticing the half that was missed.** Every `*_screen.dart` now enters, and `test/shared/design_rules_test.dart` fails one that does not, so the row cannot silently reopen — the component gallery is the single exemption and carries its reason. Seven screens had no entrance at all (active sessions, session expired, both legal screens, both not-built placeholders) plus all three onboarding steps, and they have one now. **The more useful finding was that a screen could pass the check and still be wrong.** `Profile` was already wrapped and read as a single block, because `FadeUpColumn` had been handed its two *containers* rather than its ten rows — a stagger of one step. The mechanism was present, the effect was absent, nothing failed, and it took a person looking at the app to say so. `fadeUpAll` now skips gaps (a childless `SizedBox` has nothing to fade and was consuming a stagger step), `startIndex` continues a run across an outer widget, and `Profile`'s own test asserts the indices form a run — which is the only kind of test that catches a shallow entrance. `frontend/CLAUDE.md` carries the rule as *stagger the rows a reader sees, never the boxes they sit in*. No layout moved: 471 frontend tests and `flutter analyze` are unchanged. **What is still unverified is what it looks like**, which no widget test can say — the emulator misreports frame timing (HANDOVER) and the device pass is P9-2's. |
| P6A-2 | **That the handoff opens the wizard on step 1 with a genuinely fresh draft.** §Phase 6a's step 4 is "hand off directly into the Phase 9 wizard's Step 1, pre-populated with nothing (a fresh draft)". The handoff was built and asserted against the real route table, but what opened behind `/services/new` could not be asserted, because there was no wizard and no `Listing` table. | Phase 9 | 2026-09-14 | **`/services/new` is the wizard now, and `test/features/onboarding/phase6a_done_when_test.dart` asserts both halves through the real app and its real route table.** Arriving from onboarding's confirmation sheet lands on `ServiceWizardScreen` at "Step 1 of 7 · Details" — not `BecomeProviderScreen`, not a placeholder. **Fresh, not resumed:** the only call under `/v1/providers/me/listings` is the `POST` that creates it, nothing listed the provider's drafts and nothing read one by id, and the service-name controller is empty — so there was no most-recent draft for it to reopen. **Step 2 pre-fills from the account-level `ProviderServiceArea`** §Phase 7 built: the wizard reads `GET /v1/providers/me` and PATCHes `serviceAreaIslandIds` onto the new listing by **id**, once, at creation (§0.0 item 12 — never by name), and the Location step then renders that island's qualified display name. The two sets stay distinct, which is ledger **P7-3**'s point: the copy runs one way and the account default is never read again. The header reading **5** required fields left rather than 6 on arrival is what proves the pre-fill actually landed.
| P5-1 | **`findVisibleProviders` includes a provider the moment a real listing is published.** §Phase 5 is sequenced before §Phase 8, so there was no `Listing` table to publish and every assertion ran against the `PublishedListingSource` seam (`FakeListings`). | Phase 8 | 2026-09-10 | **Implemented over the real table and re-run against published rows.** `PUBLISHED_LISTINGS` is now the default wired in `app.ts`, so `test/listings-visibility.test.ts` exercises the app that ships with nothing injected: a provider with only a draft is absent from `findVisibleProviders` (walked to the last cursor, not just page one) and `isVisible` is false; `POST …/publish` puts them in with no change to the provider row; `hidden_by_provider` takes them out and `active` puts them back — the round trip v1's stored flag could not represent; a soft-deleted listing drops them; and a suspended provider with a live listing is still excluded from the page, from `isVisible` and from `readPublic`. `information_schema` is re-asserted over **both** tables now that a second one exists: no `lifecycle_status`, `is_visible`, `published_listing_count` or `provider_is_visible` anywhere, while the listing's own `status` and `visibility` are present as the derivation's inputs. **The batch loop is gone**, as decision 2 of `docs/decisions/17-phase-5-provider-profiles.md` asked: `PublishedListingSource` now returns a `Prisma.ProviderProfileWhereInput` instead of a `Set`, so a page is one indexed query with `take: limit + 1` and the whole rule — suspension, account status, caller filters, the published-listing requirement — is one SQL statement. No caller of `findVisibleProviders` changed. `FakeListings` was reshaped to `id IN (…)` and kept, because a test of the visibility *rule* should not have to build a publishable listing to say "this provider has one"; the Phase 5 test that guarded the loop's batching is replaced by one asserting a page comes back **full** when two candidates in three fail the rule. |
| P6A-3 | That a provider with live listings is never routed back into onboarding. `providerOnboardingComplete` was derived from five legally-editable fields, so clearing a bank field or removing the last account-level island read *incomplete* and §Phase 6's switcher sent a trading provider back into a flow whose last step opens a fresh wizard draft. | Owner's decision, 2026-09-10 | 2026-09-10 | **Resolved by design rather than deferred to Phase 9/10.** The row proposed waiting for `PublishedListingSource` so the rule could be "a provider holding a published listing routes to the dashboard regardless" — but §Phase 6a's Done-when asks a *past-tense* question, "a provider who **already completed** onboarding never sees it again", and no predicate over current values can answer it. `ProviderProfile.onboardingCompletedAt` now records the moment the requirements were first met, written once by the two writes that can meet them — the profile update and the service-area add — and never cleared. The read is `stamped OR meets-now`, so the answer is correct before the stamp lands and monotonic after. The §1a analogy the derivation rested on does not hold: §1a derives *visibility*, present tense and required to change; this is history. `meetsOnboardingRequirements` remains exported for the present-tense question, which is the one a later phase asks to nudge a provider whose bank details have gone missing. Three Phase 6a tests that asserted the non-monotonicity are rewritten, including the one asserting `information_schema` held no such column — it now asserts exactly one. |
| P7-1 | The Flutter island controls rendering the live endpoint's response on a device. The search rules were verified server-side over HTTP; what was unverified was the widget drawing them. | Phase 7 device pass | 2026-09-10 | Emulator, debug build against `API_BASE_URL=http://10.0.2.2:3000`, dev database seeded to 192 islands. **Gallery → Island picker:** `mee` drew `Dh. Meedhoo` / `R. Meedhoo` / `S. Meedhoo`, each with its atoll beneath it, then `Hangnaameedhoo` last — the internal match ranked below the three prefix matches, on the device and not only in the query. `gdh` listed Gaafu Dhaalu (Faresmaathodaa, Fiyoari, Gahdhoo, Hoan'dehdoo) unqualified, since those names are unique. `male` found `Male'` and `Hulhumale'`, so the accent and apostrophe folding survives the round trip. Unsearched, the list drew the register with apostrophes intact (`An'golhitheemu`, `Ban'didhoo`). **Explore:** the pill opened the sheet, picking `S. Meedhoo` closed it and the pill read "Browsing S. Meedhoo" — qualified, not bare — and it survived a round trip to the Bookings placeholder and another to Profile. Independently re-checked over HTTP: `dhoo` returns 105 uncapped, empty returns 192, `vilingili` returns both `GA. Vilin'gili` and `K. Vilingili`. **The pass also found a defect**, fixed alongside: the Profile nav tab pushed Profile for a guest, which 401'd and drew "Couldn't load your profile". |
| P3 | A password-reset attempt for an unverified email is refused without revealing existence. | Phase 3b | 2026-09-08 | `PasswordResetService` calls `assertRecoverableByEmail` and turns the throw into a silent non-send. `backend/test/auth-password-reset.test.ts` asserts it three ways: an unregistered address and a live one return byte-identical bodies with the same status; an unverified address returns that same body, receives no mail and creates no `email_otp` row; and an unknown address fails `verify` with the same `OTP_EXPIRED` a stale code gets. A frozen account is covered by the same path. |
| P8A-1 | **What an "appeal" against a rejected payment actually is.** §1b step 5 and §Phase 10a both offer a rejected provider "resubmit **or** appeal", and §Phase 10a's Done-when tests "working resubmit and appeal" — but no section said what an appeal *changes*, §Phase 8a's endpoint list did not include one, and nothing held its state. Resubmit was met; appeal was deliberately not built, because inventing whether it is a status, a flag or a note would have put semantics in the schema that no decision backed. | Phase 10a part 1, owner's decision 2026-09-15 | 2026-09-15 | **An appeal is a re-review request stamped on the rejected row, and §1b step 5 is what settled it** — "resubmit immediately — no cooldown — or **appeal for re-review**". The plan did say what an appeal is *for*; what it never said was what it changes, and the answer is: nothing about the payment. `POST /v1/providers/me/payment-submissions/:id/appeal` sets `appealedAt` and an optional `appealNote`; the status stays `rejected`, the entitlement stays exactly what a rejection left, and §Phase 10a part 2's queue lists a rejected row carrying `appealedAt` with no later decision. **The row's own prediction held**: "a new status on `PaymentSubmission` is the least likely right answer — an appeal is a case somebody works, not a state a payment sits in." One appeal per submission (`PAYMENT_APPEAL_ALREADY_FILED` names the first one's date); a **reversal** lands the row as `rejected` and is appealable the same way; resubmit and appeal are independent, so paying again does not withdraw the appeal and appealing does not block a fresh submission. Audit-logged as the **provider**, `payment_submission.appealed`, with the note deliberately absent from the metadata — it is free text about a real decision and audit metadata is ids, enums and counts. `backend/test/phase10a-part1.test.ts` asserts all of it including the wrong-user 404 and the idempotent replay; `frontend/test/features/billing/phase10a_part1_done_when_test.dart` drives the provider's side through the real route table. The **admin** outcomes on an appealed row — overturn, or uphold with a reason — are Part 2's and are part of row **P10A-1**. |
| P8A-4 | **That the `acceptingNewCustomers` toggle tells a provider what it does to their billing.** §1b's pause keys off the toggle and a clock that starts under an already-off toggle starts paused, so the setting has a billing consequence. Resolved for §Phase 6a (no subscription there, so nothing to describe) and answered for §Phase 10 (its dashboard renders no toggle); the sentence was owed by §Phase 10a's billing screen alone, beside a live subscription. | Phase 10a part 1 | 2026-09-15 | **It is in the pause card on `BillingScreen`, which is where the effect renders beside a live subscription**, and it says all three things the row asked for: that pausing is *the same switch* as "Accepting new customers" in the provider profile, that either door spends **this subscription's ten pause days, which do not refill**, and that the **billing anchor moves** by however long the pause lasted. `frontend/test/features/billing/billing_screen_test.dart` asserts the three phrases are on the card rather than asserting the card exists. 🔧 **One departure from the artboard, in the plan's favour:** `Billing.dc.html` draws the pause control on the premium state only, and the screen renders it during the **trial** as well — §1b's "the identical mechanism applies to the trial period — one function, one cap, one resume rule, shared by `trialing` and `active`", and a control that appeared only once a provider was paying would hide something the trial has. The accepted residue named in the row is unchanged and still accepted: a provider who turns the toggle off during §Phase 6a's onboarding starts a later trial paused without having been told, bounded at ten days and in their favour. |
| P2 | A deletion request with an open booking completes automatically when that booking terminates. Phase 3 built the `DeletionBlocker` seam and tested it with an injected blocker. | Phase 17.1 | 2026-09-16 | **The real blocker is what `app.ts` now wires, and the row's own test was written as it asked.** `bookingDeletionBlocker` answers over the `booking` table and `neverBlocks` is no longer imported anywhere in the app. `test/phase17-1-seams.test.ts` creates an accepted booking, freezes the customer with a deadline 30 days out so only the booking can hold it, runs the real `anonymise-accounts` job and sees `anonymisedAt` still null; cancels the booking, runs the job again, and sees it set. It blocks on **both** sides of the relationship — a provider with an accepted job owes somebody a visit and a customer with one is owed it — and `NON_TERMINAL_STATUSES` deliberately includes `payment_unresolved` and `disputed`, because §1c says an admin still owes somebody an answer on each and a deletion completing under an open dispute would anonymise one side of the evidence. **The interface did not change**, and no caller in `modules/account/` did either. |
| P9A-2 | **That §Phase 17.1 calls the reservation from *inside* its booking transaction** — rather than reserving beside it and leaving a window. §Phase 9a proved the guarantee itself under real concurrency but could not prove the booking machine's own transaction, because there was no booking machine. | Phase 17.1 | 2026-09-16 | **One transaction, and the row named the only assertion that distinguishes it from two.** `createSlotBooking` opens a single `$transaction` around `reserveSlot`, the booking row and its first status event. `test/phase17-1-done-when.test.ts` makes the booking write fail **after** the reservation has succeeded — by pointing `islandId` at a uuid no island has, so the foreign key refuses the insert — and asserts no booking, **no unreleased hold**, and the slot back at `open`. Reserving in its own transaction would have left the hold behind and the slot reserved forever. The same file re-proves the rest through the real HTTP endpoint rather than the seam: two genuinely concurrent `POST`s on one slot resolve to exactly one 201 and one 409 `SLOT_NO_LONGER_AVAILABLE` with one booking row; and a second listing of the **same provider**, at the same time, is refused `PROVIDER_TIME_UNAVAILABLE` — the cross-listing case the exclusion constraint exists for, now asserted through the booking endpoint that will actually meet it. |
| P9A-1 | **The two halves of §Phase 9a's screens that need a `Booking`** — `Pick a Time.dc.html` below the time grid, and `My Calendar.dc.html`'s "Upcoming commitments", which renders a customer name, a booking reference and a mode chip that do not exist on a `Reservation`. | Phase 17.1 | 2026-09-16 | **Both halves, as the row asked.** `BookSlotScreen` pushes `SlotPickerScreen`, takes the `PickedSlot` it pops, and continues into the rest of the artboard — address, job notes, the email-verification gate stated rather than hidden, and Confirm — then `pushReplacement`es to the booking it created, because going "back" to a form already sent is an invitation to send it twice. The refusals land where the customer can act on them: `SLOT_NO_LONGER_AVAILABLE` clears the chosen time so the next tap is a fresh choice rather than a retry of a gone one. **My Calendar carries all three fields now**: `findHeldReservations` joins the booking through its Prisma relation — not a call into the bookings module — and the row renders the customer's name, the reference and a mode chip, degrading to §Phase 9a's own time-only row where a hold has no booking behind it (a provisional quote hold). `test/phase17-1-done-when.test.ts` books a published slot end to end and reads `GET /v1/providers/me/calendar` back with the reference, the mode and the name on it — and asserts that endpoint, the one place this phase joins a `User` row, still carries no phone number. |
