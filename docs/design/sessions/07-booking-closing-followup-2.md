# Session 6 corrections, round 2 — the same sentence, one state later

Five of six landed exactly. Book Again now says Mariyam still has to accept, the emergency re-send describes offers arriving together with callout fee and arrival estimate, the "No" answer is pilled `disputed` rather than `unresolved`, the message reason is **Asking for contact details** with a sample that actually solicits a number, and the series header prices by the visit.

The sixth was fixed in one of the two places it appears.

---

## Raise a dispute — the submitted state still has the shared record

The "What RaajjePro holds" card on the form is now correct:

> An admin reviews exactly that, and patterns across bookings count against a provider.

But the `isDone` block, which the customer reads immediately after tapping *Raise this dispute*, kept the original sentence:

> Recorded as "…". An admin will look at it, and **both sides see the same record** — terms, amendments, chat, and attestations. You'll hear back here.

Same invented capability, and arguably in the worse position: this is the screen a customer sits on while deciding whether raising the dispute was worth it, so it is the sentence they will remember and expect the product to honour. The record goes to the **admin** with both parties' history shown together; there is no shared dispute file the two of them read.

Cut the clause, exactly as on the form:

> Recorded as "…". An admin will look at it, and you'll hear back here.

Nothing else on that state changes — the `disputed` pill, the completed-booking note beneath it, and the back link are all correct.

---

## Leave alone

Everything else across all seven screens. The five round-1 fixes are right as applied and should not be revisited, and none of the untouched content — the two-tap rate flow, the three severities' visual separation, the per-target report reason sets, Book Again's delisted state, the recurring skip-versus-end split — needs anything.

This should be the last round on session 6.
