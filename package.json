{
  "name": "raajjepro",
  "version": "0.0.0",
  "description": "RaajjePro monorepo — root holds only the commit hooks; the apps live in backend/ and frontend/",
  "scripts": {
    "prepare": "husky",
    "verify": "scripts/verify.sh"
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "husky": "9.1.7",
    "lint-staged": "17.4.1"
  }
}
