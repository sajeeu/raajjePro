# RaajjePro — Flutter app

Customer and provider app. Conventions are in `CLAUDE.md` in this directory; the layout is described in `lib/README.md`.

```bash
flutter pub get
flutter run            # a connected device or emulator
flutter analyze        # must be clean
flutter test
```

Store identity: Android `applicationId` and iOS bundle identifier are both `mv.raajjepro.app`.
